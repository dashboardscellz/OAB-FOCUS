# OAB Focus — correção da tela branca

Versão simplificada para GitHub Pages: CSS e JavaScript do site estão embutidos no `index.html`, evitando erros de pastas `css/` e `js/`.

Envie os ARQUIVOS de dentro desta pasta para a RAIZ do repositório OAB-FOCUS. Em Settings > Pages use `main` e `/(root)`.

O JSON privado de conteúdo NÃO vai para o GitHub. Ele é importado pelo painel Admin para o Firebase.
